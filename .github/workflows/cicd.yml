name: CI/CD pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: Build with Maven
        run: mvn -B package --file pom.xml -DskipTests
      - run: mkdir staging && cp target/*.jar staging
      - name: Log in to Dockerhub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Push docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: true
          secrets: |
            "SPRING_PASSWORD=${{ secrets.SPRING_PASSWORD }}"
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/bookcrud-cicd-aws:test
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Set private key
        run: |
          echo "${{ secrets.AWS_PRIVATE_KEY }}" > key.pem
          chmod 600 key.pem
      - name: Log in to docker
        run: |
          ssh -o StrictHostKeyChecking=no -i key.pem ec2-user@bookcrud-cicd-aws.duckdns.org 'sudo docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}'
      - name: Pull docker image
        run: |
          ssh -o StrictHostKeyChecking=no -i key.pem ec2-user@bookcrud-cicd-aws.duckdns.org 'sudo docker pull koffarn/bookcrud-cicd-aws:latest'
      - name: Stop and remove old container
        run: |
          ssh -o StrictHostKeyChecking=no -i key.pem ec2-user@bookcrud-cicd-aws.duckdns.org 'sudo docker stop bookcrud-cicd-aws || true'
          ssh -o StrictHostKeyChecking=no -i key.pem ec2-user@bookcrud-cicd-aws.duckdns.org 'sudo docker rm bookcrud-cicd-aws || true'
      - name: Run new container
        run: |
          ssh -o StrictHostKeyChecking=no -i key.pem ec2-user@bookcrud-cicd-aws.duckdns.org 'sudo docker run -d -p80:8080 --name bookcrud-cicd-aws -e "SPRING_PASSWORD=${{ secrets.SPRING_PASSWORD }}" koffarn/bookcrud-cicd-aws:latest'